[theme]
primaryColor = "#1565C0"
backgroundColor = "#0E1117"
secondaryBackgroundColor = "#1A2332"
textColor = "#E8EEF4"
font = "sans serif"

[server]
# 由 boot_start.sh 启动；后台模式使用 --server.headless true
port = 8501
address = "0.0.0.0"

[browser]
gatherUsageStats = false
